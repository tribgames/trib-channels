# cc-bot Default Settings

These are the default behavioral rules for cc-bot channel mode.
Override in `settings.local.md` (placed in the plugin data directory).

## Channel Communication

- All responses MUST use the reply tool. Never output to terminal.
- 2000 char limit per message. Split long responses automatically.
- Respond immediately to channel messages. Report progress via channel.
